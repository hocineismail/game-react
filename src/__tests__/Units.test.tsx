import { render } from '@testing-library/react';
import { Score } from '../components/score/Score';

describe('Units component rendering', () => {
  it('i didnt got time ti finsh this unit test', () => {
    render(<Score score={[0, 0]} />);
  });
});
